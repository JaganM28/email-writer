services:
  - type: web
    name: email-writer-api
    plan: free
    # Change the runtime to 'docker'
    runtime: docker
    # The root directory is still 'backend' because that's where the Dockerfile is
    rootDir: backend
    # We no longer need buildCommand or startCommand because the Dockerfile handles it.